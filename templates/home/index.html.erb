<div class="flex min-h-screen flex-col items-center justify-center px-6 py-12">
  <div class="mx-auto max-w-lg text-center">
    <h1 class="font-display text-5xl text-olive-950 sm:text-6xl dark:text-olive-50">
      <%= Rails.application.class.module_parent_name %>
    </h1>

    <p class="mt-6 text-lg text-olive-600 dark:text-olive-400">
      Your app has been forged. Start building something great.
    </p>

    <div class="mt-10 flex items-center justify-center gap-x-6">
      <% if user_signed_in? %>
        <span class="text-sm text-olive-700 dark:text-olive-300">
          Signed in as <strong class="font-medium"><%= current_user.email %></strong>
        </span>
        <%= button_to "Sign out", destroy_user_session_path, method: :delete,
          class: "rounded-lg bg-olive-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-olive-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-olive-600 transition-colors duration-150 cursor-pointer" %>
      <% else %>
        <%= link_to "Sign in", new_user_session_path,
          class: "rounded-lg bg-olive-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-olive-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-olive-600 transition-colors duration-150" %>
        <%= link_to "Sign up", new_user_registration_path,
          class: "text-sm font-semibold text-olive-600 hover:text-olive-500 dark:text-olive-400 dark:hover:text-olive-300" %>
      <% end %>
    </div>
  </div>
</div>
